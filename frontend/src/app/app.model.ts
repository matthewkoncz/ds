import { Data } from '@angular/router';

export interface UserData {
  firstName?: string;
  lastName?: string;
  email?: string;
  phone?: string;
  birthday?: string;
  about?: string;
  avatar?: string;
  dateOfChange?: string;
}
